import { AsFormGroupPipePipe } from './as-form-group.pipe';

describe('AsFormGroupPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AsFormGroupPipePipe();
    expect(pipe).toBeTruthy();
  });
});
